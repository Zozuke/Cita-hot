<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes">
  <title>Vínculo · Encuentra conexiones reales</title>
  <!-- Fuentes elegantes -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600;14..32,700&display=swap" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    /* ---------- RESET & VARIABLES ---------- */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(145deg, #f5f7fc 0%, #edf0f8 100%);
      color: #1a1c29;
      line-height: 1.5;
      padding: 20px 16px;
      min-height: 100vh;
      display: flex;
      align-items: flex-start;
      justify-content: center;
    }

    .app-container {
      max-width: 1280px;
      width: 100%;
      margin: 0 auto;
    }

    /* ---------- PANTALLA DE REGISTRO/LOGIN ---------- */
    .auth-container {
      max-width: 480px;
      margin: 60px auto;
      background: white;
      border-radius: 40px;
      padding: 40px 36px;
      box-shadow: 0 30px 50px -15px rgba(0,0,0,0.1);
      border: 1px solid rgba(255,255,255,0.5);
    }

    .auth-header {
      text-align: center;
      margin-bottom: 36px;
    }

    .auth-logo {
      font-size: 2.8rem;
      font-weight: 700;
      background: linear-gradient(145deg, #1e1f2e, #6b4f8c);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 8px;
    }

    .auth-tabs {
      display: flex;
      gap: 16px;
      margin-bottom: 32px;
      border-bottom: 1px solid #e8ebf2;
      padding-bottom: 12px;
    }

    .auth-tab {
      flex: 1;
      text-align: center;
      padding: 12px;
      font-weight: 600;
      color: #7a7f9a;
      cursor: pointer;
      border-radius: 30px;
      transition: 0.2s;
    }

    .auth-tab.active {
      background: #1e1f2e;
      color: white;
    }

    .auth-form input {
      width: 100%;
      padding: 16px 20px;
      border: 1.5px solid #e2e6f0;
      border-radius: 20px;
      margin-bottom: 18px;
      font-size: 1rem;
      font-family: 'Inter', sans-serif;
    }

    .auth-form button {
      width: 100%;
      padding: 18px;
      background: #1e1f2e;
      color: white;
      border: none;
      border-radius: 30px;
      font-weight: 600;
      font-size: 1.1rem;
      cursor: pointer;
      margin-top: 12px;
    }

    /* ---------- NAVBAR ---------- */
    .navbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: rgba(255,255,255,0.92);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      padding: 14px 28px;
      border-radius: 60px;
      box-shadow: 0 12px 28px -8px rgba(0,0,0,0.06);
      margin-bottom: 32px;
      border: 1px solid rgba(255,255,255,0.6);
    }

    .logo {
      font-weight: 700;
      font-size: 1.8rem;
      letter-spacing: -0.03em;
      background: linear-gradient(145deg, #1e1f2e, #5b4a6e);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .nav-links {
      display: flex;
      gap: 32px;
      font-weight: 500;
      color: #3d4055;
    }

    .nav-links span {
      cursor: pointer;
      padding: 6px 0;
      border-bottom: 2px solid transparent;
      transition: all 0.2s;
      font-size: 1rem;
    }

    .nav-links span.active {
      border-bottom-color: #b87aff;
      color: #1a1c29;
      font-weight: 600;
    }

    .profile-badge {
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .user-greeting {
      font-weight: 500;
      color: #4a3a5c;
    }

    .premium-chip {
      background: #f1eaff;
      color: #5e3c8a;
      padding: 7px 16px;
      border-radius: 40px;
      font-size: 0.8rem;
      font-weight: 600;
      border: 1px solid #dfc9ff;
      cursor: pointer;
    }

    .logout-btn {
      background: none;
      border: none;
      color: #8a7e9e;
      cursor: pointer;
      font-size: 1.2rem;
      padding: 6px;
    }

    /* ---------- VISTAS ---------- */
    .view {
      display: none;
      opacity: 0;
      transition: opacity 0.25s;
    }

    .view.active {
      display: block;
      opacity: 1;
    }

    .main-app {
      display: none;
    }

    .main-app.visible {
      display: block;
    }

    /* Tarjetas */
    .card {
      background: #ffffff;
      border-radius: 32px;
      padding: 28px 32px;
      box-shadow: 0 20px 35px -10px rgba(0,0,0,0.05);
      border: 1px solid #edf0f5;
    }

    h2 {
      font-weight: 650;
      font-size: 2rem;
      letter-spacing: -0.02em;
      margin-bottom: 24px;
      color: #1e1f2c;
    }

    /* Grid de perfiles */
    .profiles-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 26px;
    }

    .profile-card {
      background: white;
      border-radius: 28px;
      overflow: hidden;
      box-shadow: 0 15px 30px -10px rgba(0,0,0,0.04);
      border: 1px solid #ebecf0;
      transition: transform 0.15s, box-shadow 0.2s;
    }

    .profile-card:hover {
      box-shadow: 0 25px 40px -14px rgba(106, 72, 183, 0.08);
    }

    .profile-img {
      width: 100%;
      height: 250px;
      object-fit: cover;
      background: #eef1f6;
    }

    .blur-img {
      filter: blur(14px);
    }

    .profile-info {
      padding: 20px 18px 18px;
    }

    .profile-name {
      font-weight: 700;
      font-size: 1.4rem;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .profile-country {
      background: #f4f2fa;
      padding: 4px 12px;
      border-radius: 40px;
      font-size: 0.75rem;
      font-weight: 500;
      color: #4f3c70;
    }

    .profile-desc {
      margin: 8px 0 18px;
      color: #4a4e67;
      font-size: 0.95rem;
    }

    .action-buttons {
      display: flex;
      gap: 12px;
    }

    .btn {
      border: none;
      background: none;
      font-weight: 600;
      padding: 12px 18px;
      border-radius: 50px;
      font-size: 0.95rem;
      cursor: pointer;
      transition: 0.2s;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      border: 1px solid transparent;
    }

    .btn-primary {
      background: #1e1f2e;
      color: white;
      box-shadow: 0 8px 16px rgba(30,31,46,0.08);
    }

    .btn-like {
      background: #feeef6;
      color: #cf3896;
      border-color: #ffd9ec;
      flex: 1;
    }

    .btn-pass {
      background: #f2f4f9;
      color: #4f536e;
      flex: 1;
    }

    /* Filtro */
    .filter-bar {
      display: flex;
      gap: 20px;
      align-items: center;
      margin-bottom: 28px;
      flex-wrap: wrap;
    }

    select, input {
      padding: 13px 22px;
      border-radius: 40px;
      border: 1px solid #e0e3ea;
      background: white;
      font-family: 'Inter', sans-serif;
    }

    /* Chat */
    .chat-container {
      background: white;
      border-radius: 30px;
      padding: 24px;
    }

    .messages {
      max-height: 420px;
      overflow-y: auto;
      padding-right: 6px;
      margin-bottom: 22px;
    }

    .message {
      margin-bottom: 16px;
      display: flex;
    }

    .message.user {
      justify-content: flex-end;
    }

    .bubble {
      max-width: 75%;
      padding: 14px 20px;
      border-radius: 24px;
      background: #f4f3f9;
      color: #1e1f2c;
      word-break: break-word;
    }

    .user .bubble {
      background: #1e1f2e;
      color: white;
    }

    .typing-indicator {
      display: flex;
      align-items: center;
      gap: 6px;
      padding: 12px 18px;
      background: #f4f3f9;
      border-radius: 24px;
      width: fit-content;
      margin-bottom: 16px;
    }

    .typing-dot {
      width: 8px;
      height: 8px;
      background: #9f9fb9;
      border-radius: 50%;
      animation: typing 1.4s infinite;
    }

    .typing-dot:nth-child(2) { animation-delay: 0.2s; }
    .typing-dot:nth-child(3) { animation-delay: 0.4s; }

    @keyframes typing {
      0%,60%,100% { transform: translateY(0); opacity: 0.5; }
      30% { transform: translateY(-8px); opacity: 1; }
    }

    .premium-overlay {
      background: #fff7e5;
      padding: 14px 20px;
      border-radius: 60px;
      color: #aa6f00;
      font-weight: 500;
      margin: 16px 0;
      border: 1px solid #ffe6b3;
    }

    /* Perfil usuario */
    .profile-form input, .profile-form textarea, .profile-form select {
      width: 100%;
      padding: 14px 20px;
      border: 1px solid #dfe2eb;
      border-radius: 24px;
      margin-bottom: 18px;
      font-family: 'Inter', sans-serif;
    }

    .photo-upload-area {
      border: 2px dashed #ccd2e0;
      border-radius: 28px;
      padding: 28px;
      text-align: center;
      margin-bottom: 22px;
      cursor: pointer;
      transition: 0.2s;
    }

    .photo-upload-area:hover {
      border-color: #b87aff;
      background: #fbfaff;
    }

    .preview-img {
      max-width: 180px;
      border-radius: 20px;
      margin-top: 16px;
    }

    .likes-counter {
      font-weight: 600;
      background: #ffe9f2;
      padding: 6px 16px;
      border-radius: 40px;
      color: #c43a82;
    }

    .welcome-banner {
      background: linear-gradient(135deg, #f0e6ff, #ffe6f2);
      padding: 16px 24px;
      border-radius: 40px;
      margin-bottom: 24px;
      font-weight: 500;
    }

    @media (max-width: 600px) {
      body { padding: 10px; }
      .navbar { padding: 10px 18px; }
      .auth-container { padding: 28px 20px; }
    }
  </style>
</head>
<body>
<div class="app-container">
  <!-- PANTALLA DE AUTENTICACIÓN -->
  <div id="authScreen" class="auth-container">
    <div class="auth-header">
      <div class="auth-logo"><i class="fas fa-link"></i> Vínculo</div>
      <p style="color: #6a6e8a; margin-top: 8px;">Conecta con personas increíbles</p>
    </div>
    <div class="auth-tabs">
      <div class="auth-tab active" data-tab="login">Iniciar sesión</div>
      <div class="auth-tab" data-tab="register">Registrarse</div>
    </div>
    
    <!-- FORMULARIO LOGIN -->
    <form id="loginForm" class="auth-form">
      <input type="email" id="loginEmail" placeholder="Correo electrónico" required />
      <input type="password" id="loginPassword" placeholder="Contraseña" required />
      <button type="submit">Entrar <i class="fas fa-arrow-right"></i></button>
    </form>
    
    <!-- FORMULARIO REGISTRO (oculto inicialmente) -->
    <form id="registerForm" class="auth-form" style="display: none;">
      <input type="text" id="regName" placeholder="Nombre completo" required />
      <input type="email" id="regEmail" placeholder="Correo electrónico" required />
      <input type="password" id="regPassword" placeholder="Contraseña" required />
      <input type="number" id="regAge" placeholder="Edad" min="18" required />
      <select id="regCountry" required style="width:100%; padding:16px; border-radius:20px; margin-bottom:18px; border:1.5px solid #e2e6f0;">
        <option value="">País</option>
        <option value="México">México</option>
        <option value="España">España</option>
        <option value="Argentina">Argentina</option>
        <option value="Colombia">Colombia</option>
        <option value="Chile">Chile</option>
      </select>
      <button type="submit">Crear cuenta <i class="fas fa-user-plus"></i></button>
    </form>
    <p style="text-align: center; margin-top: 20px; color: #8e8ea6; font-size: 0.85rem;">
      Al continuar, aceptas nuestros términos y política de privacidad.
    </p>
  </div>

  <!-- APLICACIÓN PRINCIPAL (oculta hasta login) -->
  <div id="mainApp" class="main-app">
    <!-- NAVBAR -->
    <nav class="navbar">
      <div class="logo"><i class="fas fa-link" style="color: #b87aff;"></i> Vínculo</div>
      <div class="nav-links">
        <span class="nav-item active" data-view="discover">Descubrir</span>
        <span class="nav-item" data-view="matches">Mensajes</span>
        <span class="nav-item" data-view="profile">Mi perfil</span>
      </div>
      <div class="profile-badge">
        <span class="user-greeting" id="userGreeting"></span>
        <span class="premium-chip" id="premiumBadge"><i class="fas fa-crown"></i> Premium</span>
        <button class="logout-btn" id="logoutBtn"><i class="fas fa-sign-out-alt"></i></button>
      </div>
    </nav>

    <!-- BANNER DE BIENVENIDA -->
    <div id="welcomeBanner" class="welcome-banner">
      <i class="fas fa-heart" style="color: #cf3896;"></i> 
      <span id="welcomeMessage">¡Bienvenida de vuelta!</span>
    </div>

    <!-- VISTA DESCUBRIR -->
    <div id="discover" class="view active">
      <div class="filter-bar">
        <h2 style="margin-bottom:0;">✨ Descubrir</h2>
        <div style="margin-left: auto; display: flex; gap: 16px;">
          <select id="countryFilter">
            <option value="todos">Todos los países</option>
            <option value="México">🇲🇽 México</option>
            <option value="España">🇪🇸 España</option>
            <option value="Argentina">🇦🇷 Argentina</option>
            <option value="Colombia">🇨🇴 Colombia</option>
            <option value="Chile">🇨🇱 Chile</option>
          </select>
          <span class="likes-counter"><i class="fas fa-heart"></i> <span id="likeCountValue">0</span></span>
        </div>
      </div>
      <div id="profilesContainer" class="profiles-grid"></div>
    </div>

    <!-- VISTA MATCHES / CHAT -->
    <div id="matches" class="view">
      <h2>💬 Conversaciones</h2>
      <div id="matchesList" class="profiles-grid" style="margin-bottom: 28px;"></div>
      <div id="chatSection" class="chat-container card" style="display: none;">
        <div style="display: flex; align-items: center; gap: 16px; margin-bottom: 24px;">
          <i class="fas fa-comment-dots" style="font-size: 2rem; color:#b87aff;"></i>
          <h3 id="chatWithName">Chat</h3>
        </div>
        <div id="chatMessages" class="messages"></div>
        <div id="typingIndicator" class="typing-indicator" style="display: none;">
          <span class="typing-dot"></span><span class="typing-dot"></span><span class="typing-dot"></span>
          <span style="margin-left: 6px; color: #6b6e89;">escribiendo...</span>
        </div>
        <div id="chatLimitWarning" class="premium-overlay" style="display: none;">
          <i class="fas fa-lock"></i> Límite alcanzado. <strong>Hazte Premium</strong> para chats ilimitados.
        </div>
        <div style="display: flex; gap: 12px;">
          <input type="text" id="messageInput" placeholder="Escribe un mensaje..." style="flex:1;" />
          <button class="btn btn-primary" id="sendMessageBtn"><i class="fas fa-paper-plane"></i> Enviar</button>
        </div>
      </div>
    </div>

    <!-- VISTA PERFIL -->
    <div id="profile" class="view">
      <div class="card">
        <h2><i class="fas fa-user-edit"></i> Tu perfil</h2>
        <form id="profileForm" class="profile-form">
          <input type="text" id="userName" placeholder="Nombre completo" required />
          <input type="number" id="userAge" placeholder="Edad" min="18" required />
          <select id="userCountry" required>
            <option value="México">México</option><option value="España">España</option><option value="Argentina">Argentina</option><option value="Colombia">Colombia</option><option value="Chile">Chile</option>
          </select>
          <textarea id="userDesc" placeholder="Cuéntanos sobre ti..." rows="2"></textarea>
          
          <div class="photo-upload-area" id="photoUploadArea">
            <i class="fas fa-cloud-upload-alt" style="font-size: 2rem; color: #9f7aea;"></i>
            <p style="margin: 10px 0 4px; font-weight: 500;">Subir foto de perfil</p>
            <p style="font-size: 0.8rem; color: #6f7390;">JPG, PNG (máx. 5MB)</p>
            <input type="file" id="userPhotoInput" accept="image/*" style="display: none;" />
            <img id="photoPreview" class="preview-img" style="display: none;" />
          </div>

          <button type="submit" class="btn btn-primary" style="width:100%;">Guardar cambios</button>
        </form>
        <div style="margin-top: 24px; padding: 16px; background: #faf8ff; border-radius: 24px;">
          <p><i class="fas fa-heart" style="color:#d6409f;"></i> <strong>Interacciones:</strong> <span id="profileLikes">0</span> ❤️</p>
          <p id="premiumBlurMessage" style="display:none; margin-top: 8px; color: #b45f2b;"><i class="fas fa-crown"></i> Premium: Fotos sin desenfoque</p>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  (function(){
    "use strict";

    // ---------- DOM ----------
    const authScreen = document.getElementById('authScreen');
    const mainApp = document.getElementById('mainApp');
    const loginForm = document.getElementById('loginForm');
    const registerForm = document.getElementById('registerForm');
    const authTabs = document.querySelectorAll('.auth-tab');
    const logoutBtn = document.getElementById('logoutBtn');
    const userGreeting = document.getElementById('userGreeting');
    const welcomeMessage = document.getElementById('welcomeMessage');
    
    const views = {
      discover: document.getElementById('discover'),
      matches: document.getElementById('matches'),
      profile: document.getElementById('profile')
    };
    const navItems = document.querySelectorAll('.nav-item');
    const profilesContainer = document.getElementById('profilesContainer');
    const countryFilter = document.getElementById('countryFilter');
    const likeCountSpan = document.getElementById('likeCountValue');
    const profileLikesSpan = document.getElementById('profileLikes');
    const matchesListDiv = document.getElementById('matchesList');
    const chatSection = document.getElementById('chatSection');
    const chatMessages = document.getElementById('chatMessages');
    const messageInput = document.getElementById('messageInput');
    const sendBtn = document.getElementById('sendMessageBtn');
    const chatWithName = document.getElementById('chatWithName');
    const chatLimitWarning = document.getElementById('chatLimitWarning');
    const typingIndicator = document.getElementById('typingIndicator');
    const profileForm = document.getElementById('profileForm');
    const premiumBlurMsg = document.getElementById('premiumBlurMessage');
    const photoUploadArea = document.getElementById('photoUploadArea');
    const userPhotoInput = document.getElementById('userPhotoInput');
    const photoPreview = document.getElementById('photoPreview');

    // API Keys
    const apis = [
      "AIzaSyBB0-gcgGup_9aIRD8n3o3_ZUgOBVnnTCQ",
      "AIzaSyD35-b3FybmWQvJ11H4r4TpWPJTF-5R6jw",
      "AIzaSyA3XRsjb8YgNloqDKYd9bYfXET7Fkcwk8o"
    ];
    const API_URL = "https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash-lite:generateContent";

    // Estado
    let currentUser = null;
    let fakeProfiles = [];
    let matches = [];
    let totalLikes = 0;
    let activeChatMatchId = null;
    let freeMessagesSent = 0;
    const MAX_FREE_MSG = 3;
    let blurEnabled = false;
    let typingTimer = null;

    // ---------- 16 PERFILES DIVERSOS ----------
    function generateDemoProfiles() {
      fakeProfiles = [
        // Mujeres
        { id: 'p1', name: 'Valeria', age: 27, country: 'México', desc: 'Amo los atardeceres, la música en vivo y el café de especialidad.', photo: 'https://images.pexels.com/photos/774909/pexels-photo-774909.jpeg?aut
